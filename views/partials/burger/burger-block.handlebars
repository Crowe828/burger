<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newsleep="{devour}">
		{{#if devour}}Let's eat!{{else}}I'm full!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Delete</button>
</li>
