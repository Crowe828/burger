<h1>Burgers!</h1>

<h2>Let's eat some burgers:</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burger/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers you've eaten:</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burger/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a new burger:</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Hungry?</label><br>
    <input type="radio" name="devoured" value="1" checked> Lemme eat it!<br>
    <input type="radio" name="devoured" value="0"> I've had this burger before.
  </div>

  <button type="submit">Add Burger</button>
</form>
